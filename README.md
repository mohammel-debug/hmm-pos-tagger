# hmm-pos-tagger
A Hidden Markov Model (HMM) implementation for part-of-speech tagging, trained and evaluated on two Universal Dependencies datasets: CHILDES and EWT. The notebook compares multiple model iterations, each introducing improvements over the previous, to analyse how performance evolves across both corpora.
